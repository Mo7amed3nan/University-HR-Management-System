@model List<Uni_HR_Management_System.Models.ConsolidatedLeaveViewModel>

<div class="container mt-4">
    <h2 class="mb-4 text-warning">Pending Unpaid Leaves</h2>

    @if (Model.Count == 0)
    {
        <div class="alert alert-info">No pending unpaid leaves found.</div>
    }
    else
    {
        <table class="table table-bordered table-hover">
            <thead class="bg-warning text-dark">
                <tr>
                    <th>Req ID</th>
                    <th>Start Date</th>
                    <th>End Date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.RequestId</td>
                        <td>@item.StartDate?.ToShortDateString()</td>
                        <td>@item.EndDate?.ToShortDateString()</td>
                        <td>
                            <form asp-action="ProcessUnpaidLeaves" method="post">
                                <input type="hidden" name="requestId" value="@item.RequestId" />
                                <button type="submit" class="btn btn-dark btn-sm">Process Approval</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    <a href="/HR/Index" class="btn btn-secondary mt-3">Back to Dashboard</a>
</div>